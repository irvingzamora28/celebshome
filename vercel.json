{
  "version": 2,
  "buildCommand": "npm run build:scripts && npm run vercel-build && npm run db:init",
  "installCommand": "npm install",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ]
}
